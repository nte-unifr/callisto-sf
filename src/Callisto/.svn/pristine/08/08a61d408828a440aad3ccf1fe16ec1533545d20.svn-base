{% extends "CallistoFichesBundle:Default:index.html.twig" %}

{% block callisto_content %}
    <h1>{{ entity.title }}</h1>

    <p><strong>{{ entity.introduction|raw }}</strong></p>
    <p>{{ entity.text|raw }}</p>
{% endblock %}
